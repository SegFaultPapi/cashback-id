# =============================================================================
# CASHBACK ID – Environment variables
# Copy to .env.local and fill in. Never commit .env or .env.local.
#
# Two blockchains:
#   • ETHEREUM  → ENS (subdomains *.cashbackid.eth). Deploy Solidity contract.
#   • SUI       → Profiles + payments. Use existing Move package (or deploy yours).
# =============================================================================

# -----------------------------------------------------------------------------
# ETHEREUM – ENS subdomain registrar (contracts/ in this repo)
# -----------------------------------------------------------------------------
# Private key (hex, with 0x) of the EOA that deploys CashbackIdRegistrar to
# Ethereum mainnet. Must have ETH for gas. Used for:
#   • forge script script/DeployCashbackIdRegistrar.s.sol:... --broadcast
#   • Custodial on-chain registration: if ETH_REGISTRAR_OWNER_PRIVATE_KEY is unset, this key is also used for registerFor() in /api/ens/claim-subdomain.
PRIVATE_KEY=0x...

# Optional: separate key for custodial registerFor (owner of CashbackIdRegistrar). If unset, PRIVATE_KEY is used. That wallet must have ETH for gas per subdomain.
# ETH_REGISTRAR_OWNER_PRIVATE_KEY=0x...

# Optional: verify contract on Etherscan after deploy
# ETHERSCAN_API_KEY=your_etherscan_api_key

# Optional: Ethereum mainnet RPC for forge script deploy (avoids 429 rate limit on public RPCs).
# ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/your-key
# Or: https://mainnet.infura.io/v3/your-key

# Deployed CashbackIdRegistrar on Ethereum mainnet (for on-chain subdomain registration).
NEXT_PUBLIC_ENS_REGISTRAR_ADDRESS=0x590992a59EB5b989030A75AB8f32d2DFF0c70073

# Optional: Ethereum RPC for the app (ENS resolution). If unset, uses public RPC.
# NEXT_PUBLIC_ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/your-key

# -----------------------------------------------------------------------------
# SUI – Profiles and payments (/api/pay)
# -----------------------------------------------------------------------------
# Server keypair (base64). This wallet signs /api/pay (process_payment) and
# pays gas for those txs. Must be funded with SUI (testnet or mainnet).
# "Create profile" in the app is signed by the USER (zkLogin address), so the
# user's Sui address (faucet) needs SUI for that; this key is only for the server.
SUI_PRIVATE_KEY=base64_encoded_ed25519_sui_secret_key

# Network: testnet or mainnet
NEXT_PUBLIC_SUI_NETWORK=testnet

# Move package ID (profile + checkout modules). By default we use the
# already-deployed GA-Asso/Hackmoney package on testnet – you do NOT deploy
# a Sui contract unless you publish your own Move package.
# NEXT_PUBLIC_CASHBACK_PACKAGE_ID=0xbdabfb7fb7822e83b2d8ba86d211347812bb3a6d454f64828ea3c17453f4e9aa

# Optional: custom Sui RPC
# NEXT_PUBLIC_SUI_RPC_URL=https://fullnode.testnet.sui.io:443

# Optional: canonical site URL (for Open Graph / Twitter card images; e.g. https://yourapp.vercel.app)
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

# -----------------------------------------------------------------------------
# IPFS / Filecoin proof (optional)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_WEB3_STORAGE_TOKEN=your_token
# NEXT_PUBLIC_IPFS_API_URL=https://api.web3.storage
# NEXT_PUBLIC_IPFS_GATEWAY=https://w3s.link/ipfs
